#include <stdio.h>
/**
 * main - prints its name
 * @argc: int
 * @argv: array a strings
 * Return: name of file
 */
int main(int __attribute__((__unused__))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
